
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:news="http://www.google.com/schemas/sitemap-news/0.9"
        xmlns:xhtml="http://www.w3.org/1999/xhtml"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"
        xmlns:video="http://www.google.com/schemas/sitemap-video/1.1">
    
    <!-- Homepage -->
    <url>
        <loc>{{ url_for('index', _external=True) }}</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
        <lastmod>{{ moment().format('YYYY-MM-DD') }}</lastmod>
    </url>
    
    <!-- Main Pages -->
    <url>
        <loc>{{ url_for('events', _external=True) }}</loc>
        <changefreq>hourly</changefreq>
        <priority>0.9</priority>
    </url>
    
    <url>
        <loc>{{ url_for('map', _external=True) }}</loc>
        <changefreq>hourly</changefreq>
        <priority>0.9</priority>
    </url>
    
    <url>
        <loc>{{ url_for('about', _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
    </url>
    
    <url>
        <loc>{{ url_for('contact', _external=True) }}</loc>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>
    
    <!-- Event Pages -->
    {% for event in events %}
    <url>
        <loc>{{ url_for('event_detail', event_id=event.id, _external=True) }}</loc>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
        <lastmod>{{ event.created_at.strftime('%Y-%m-%d') }}</lastmod>
    </url>
    {% endfor %}
    
</urlset>
